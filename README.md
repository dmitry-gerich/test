# simple-text-input-output-tester

A simple Python application that reads text from `stdin` and echoes it back to `stdout`.

## Usage

To run the application:
```bash
python3 app.py
```

## Testing

To run the tests:
```bash
python3 test_app.py
```
